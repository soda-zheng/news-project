---
description: 修改 demo2 代码后重启开发服务（或依赖自动重载）
alwaysApply: true
---

# 开发服务：修改代码后的处理（demo2）

## 原则

- **每次**在本仓库 `demoo/demo2` 下完成 `backend/` 或 `frontend/` 的**实质性代码修改**后，在**结束本轮回复前**应帮用户**重启前后端**（或确认已启用下方自动重载，并说明无需全量重启的情况）。

## 路径（按本机工作区）

- 后端：`d:\demoo\demoo\demo2\backend`
- 前端：`d:\demoo\demoo\demo2\frontend`

（若用户仓库克隆在其他盘符，将 `d:\demoo\demoo\demo2` 换成实际 `demo2` 根目录。）

## 推荐：后端自动重载

启动后端时设置 **`DEV_RELOAD=1`**，修改 Python 后 Flask 会**自动重启子进程**，一般不必每次手动杀进程：

```powershell
Set-Location "d:\demoo\demoo\demo2\backend"
$env:DEV_RELOAD = "1"
python app.py
```

- **仅改后端 `.py`**：若当前进程已带 `DEV_RELOAD=1`，可在回复中说明「后端已自动重载」；若用户是**无重载**的旧进程，则仍应**重启后端**。
- **改 `requirements.txt`、环境变量、端口**：必须**重启后端**（必要时重装依赖）。

## 前端

- `npm run dev` 下 **Vite 默认热更新**，**仅改前端 Vue/CSS/JS** 时通常**不必重启前端**。
- 若改了 `vite.config`、`package.json`、环境变量或 HMR 异常，再**重启前端**。

## 全量重启（与用户要求「改完就重启」一致时）

用户要求**每次改代码都重启前后端**时，用 PowerShell：

```powershell
foreach ($port in 5000, 5173) {
  Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue |
    Select-Object -ExpandProperty OwningProcess -Unique |
    ForEach-Object { Stop-Process -Id $_ -Force -ErrorAction SilentlyContinue }
}
Set-Location "d:\demoo\demoo\demo2\backend"
$env:DEV_RELOAD = "1"
Start-Process powershell -ArgumentList "-NoExit","-Command","Set-Location 'd:\demoo\demoo\demo2\backend'; `$env:DEV_RELOAD='1'; python app.py"
Start-Sleep -Seconds 1
Start-Process powershell -ArgumentList "-NoExit","-Command","Set-Location 'd:\demoo\demoo\demo2\frontend'; npm run dev"
```

或使用仓库内脚本（会开两个新窗口）：

`d:\demoo\demoo\demo2\scripts\dev-restart.ps1`

## 回复用户时

简要说明是否已重启、后端是否启用 `DEV_RELOAD`，并提示 **http://localhost:5173** 与 **http://127.0.0.1:5000**。
